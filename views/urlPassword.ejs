<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Password Protected URL</title>
</head>
<body>
    <div class="container" style="display: flex; flex-direction: column; justify-content: center; align-items: center; height: 100vh;">
        <% if (message) { %>
            <p class="message"><%= message %></p>
        <% } %>
        <form action="/<%= shortUrl %>/verify" method="POST" style="text-align: center;">
            <div style="margin-bottom: 10px;">
                <input type="password" name="password" style="border: 1px solid #3f4d5c;" required>
            </div>
            <div>
                <button type="submit">Submit</button>
            </div>
        </form>
    </div>
</body>
</html>