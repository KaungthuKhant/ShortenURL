<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Welcome, <%= name %></h1>
        <h1>URL Shrinker</h1>
        
        <form action="/shortUrls" method="POST">
            <div class="form-row">
                <input required placeholder="Enter URL to shorten" type="url" name="fullUrl" id="fullUrl">
                <input placeholder="Custom short URL (optional)" name="shortUrl" id="shortUrl">
                <button type="submit">Shorten</button>
            </div>
        </form>
        
        <table>
            <thead>
                <tr>
                    <th>Full URL</th>
                    <th>Short URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% urls.forEach(shortUrl => { %>
                    <tr>
                        <td><a href="<%= shortUrl.fullUrl %>"><%= shortUrl.fullUrl %></a></td>
                        <td><a href="<%= shortUrl.shortUrl %>"><%= shortUrl.shortUrl %></a></td>
                        <td><%= shortUrl.clicks %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
        
        <form action="/logout?_method=DELETE" method="POST" class="logout-form">
            <button type="submit" class="logout-button">Log Out</button>
        </form> 
    </div>
</body>
</html>