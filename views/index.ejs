<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container-fluid p-5">
        <h1>Hi <%= name %></h1>
        <h1>URL Shrinker</h1>
        
        <form action="/shortUrls" method="POST" class="my-4 w-100">
            <div class="row g-3 align-items-center">
                <!-- URL input -->
                <div class="col">
                    <label for="fullUrl" class="visually-hidden">Url</label>
                    <input required placeholder="Url" type="url" name="fullUrl" id="fullUrl" class="form-control">
                </div>

                <!-- Short URL input -->
                <div class="col">
                    <label for="shortUrl" class="visually-hidden">Short URL</label>
                    <input placeholder="Short Url" name="shortUrl" id="shortUrl" class="form-control">
                </div>

                <!-- Submit button -->
                <div class="col-auto">
                    <button class="btn btn-success" type="submit">Shorten</button>
                </div>
            </div>
        </form>
        
        <!-- The rest of your content remains unchanged -->
        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th>Full URL</th>
                    <th>Short URL</th>
                    <th>Clicks</th>
                </tr>
            </thead>
            <tbody>
                <% urls.forEach(shortUrl => { %>
                    <tr>
                        <td><a href="<%= shortUrl.fullUrl %>"></a><%= shortUrl.fullUrl %></td>
                        <td><a href="<%= shortUrl.shortUrl %>"><%= shortUrl.shortUrl %></a></td>
                        <td><%= shortUrl.clicks %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>

        <form action="/logout?_method=DELETE" method="POST">
            <button type="submit">Log Out</button>
        </form> 
    </div>
</body>
</html>
