<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1>Hi <%= name %></h1>
        <h1>URL Shrinker</h1>
        <form action="/shortUrls" method="POST" class="my-4 form-inline">
            <label for="fullUrl" class="visually-hidden">Url</label>
            <input required placeholder="Url" type="url" name="fullUrl" id="fullUrl">

            <label for="shortUrl" class="visually-hidden">shortUrl</label>
            <input placeholder="Short Url" name="shortUrl" id="shortUrl">

            <button class="btn btn-success" type="submit">Shorten</button>
        </form>

        <table class="table table-striped table-responsive">
            <thead>
                <tr>
                    <th>Full URL</th>
                    <th>Short URL</th>
                    <th>CLicks</th>
                </tr>
            </thead>
            <tbody>
                <% urls.forEach(shortUrl => { %>
                    <tr>
                        <td><a href="<%= shortUrl.fullUrl %>"></a><%= shortUrl.fullUrl %></td>
                        <td><a href="<%= shortUrl.shortUrl %>"><%= shortUrl.shortUrl %></a></td>
                        <td><%= shortUrl.clicks %></td>
                    </tr>
                <% }) %>

                
            </tbody>
        </table>

        <form action="/logout?_method=DELETE" method="POST">
            <button type="submit">Log Out</button>
        </form> 
    </div>
</body>
</html>


